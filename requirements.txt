# AIDEFEND MCP Service Dependencies
# All versions pinned for security and reproducibility

# FastAPI and server
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9

# Pydantic and settings
pydantic==2.6.1
pydantic-settings==2.1.0

# Vector database
lancedb==0.5.7

# Embedding and ML (lightweight ONNX-based)
fastembed==0.2.6

# MCP (Model Context Protocol) SDK
mcp==0.9.0

# HTTP client
httpx==0.26.0

# Rate limiting
slowapi==0.1.9

# Python standard library backports (if needed for 3.9)
typing-extensions==4.9.0

# Security
# (No additional packages needed - using stdlib + our custom security module)
