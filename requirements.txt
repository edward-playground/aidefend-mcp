# AIDEFEND MCP Service Dependencies
# Requires Python >= 3.10 (mcp==1.29.0 and ipython require >=3.10).
# The release matrix covers Python 3.10 through 3.14.
# Last updated: 2026-08-06

# FastAPI and server
fastapi==0.141.1
starlette==1.4.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32

# Pydantic and settings
pydantic==2.13.4
pydantic-settings==2.14.2

# Vector database
lancedb==0.25.3

# Apache Arrow tables are imported directly by app/sync.py. Match LanceDB's
# supported floor instead of relying on its transitive dependency declaration.
pyarrow>=16

# Data processing (required by LanceDB for .to_pandas())
pandas>=2.0.0

# Numerical arrays (imported directly in app/embedding_cache.py; also a transitive dep of
# pandas/lancedb/fastembed, declared explicitly so it can never go missing on a fresh install)
numpy>=1.24,<3

# Embedding and ML (lightweight ONNX-based)
fastembed==0.8.0
pillow==12.3.0
# Imported directly by the installer and Docker runtime contract. These markers
# mirror the pinned FastEmbed 0.8.0 backend compatibility ranges for every
# Python version supported by this project.
onnxruntime>=1.17.0,!=1.20.0,<1.24; python_version == '3.10'
onnxruntime>=1.17.0,!=1.20.0,!=1.24.0,!=1.24.1; python_version >= '3.11' and python_version < '3.13'
onnxruntime>1.21.0,!=1.24.0,!=1.24.1; python_version == '3.13'
onnxruntime>=1.24.2; python_version >= '3.14'

# MCP (Model Context Protocol) SDK
mcp==1.29.0

# Structured cancellation shielding used by cancellation-safe worker drains
anyio==4.14.2

# Windows-specific: MCP SDK requires pywin32 for Windows platform APIs
pywin32==312; sys_platform == 'win32'

# HTTP client
httpx==0.28.1

# Rate limiting
slowapi==0.1.10

# Python standard library backports
typing-extensions==4.16.0

# HTML parsing
beautifulsoup4==4.15.0

# Optimized fuzzy string matching
rapidfuzz==3.14.5

# Read-Write Lock for QueryEngine
aiorwlock==1.5.1

